# Copy this file to `scrapper/_secrets.py` and fill in your real secrets.
# This file is safe to keep in the repo; do NOT commit the real `scrapper/_secrets.py`.

# Example values (replace with your actual credentials in scrapper/_secrets.py):
GOOGLE_API_KEY = "YOUR_GOOGLE_API_KEY"
GOOGLE_CX = "YOUR_CUSTOM_SEARCH_ENGINE_ID"
